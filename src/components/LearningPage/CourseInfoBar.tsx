import type{Lesson}from'../../types';import type{SubCourse}from'../../data/courses';import{getLessonMeta}from'../../data/lessonMeta';
export function CourseInfoBar({lesson,currentLessonId,onSwitchLesson,lessonList,currentStep}:{lesson:Lesson;currentLessonId:string;onSwitchLesson:(id:string)=>void;lessonList:SubCourse[];currentStep:number}){const meta=getLessonMeta(lesson.id);return <div className="course-info-bar"><div className="course-current"><span className="course-current-icon">{lesson.icon}</span><div className="course-current-text"><h3>{lesson.title}</h3><p>{meta.objective}</p><small className="progress-label">训练进度 · {currentStep} / 7 步</small></div></div><div className="switch-course"><select className="switch-btn" value={currentLessonId} onChange={e=>onSwitchLesson(e.target.value)}>{lessonList.map(l=><option key={l.id} value={l.id}>{l.titleEn}</option>)}</select></div></div>}
