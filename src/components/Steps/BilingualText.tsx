import type { Lesson } from '../../types';
import { getLessonSummary } from '../../data/lessonSummaries';
export function BilingualText({lesson}:{lesson:Lesson}){const english=lesson.bilingualText.english.trim().split(/\n\s*\n/).filter(Boolean);const chinese=lesson.bilingualText.chinese.trim().split(/\n\s*\n/).filter(Boolean);return <div className="content-card"><h2 className="card-title">双语理解</h2><p className="section-guidance">逐段对照英文和中文，先理解内容，再观察每一段在回答中的作用。</p><div className="summary-panel"><p className="card-kicker">本课重点</p><p>{getLessonSummary(lesson.id)}</p></div><div className="bilingual-reading">{english.map((paragraph,index)=><article key={index}><div><span>EN · {index+1}</span><p>{paragraph}</p></div><div><span>中文翻译</span><p>{chinese[index]||'本段翻译待补充。'}</p></div></article>)}</div></div>}
