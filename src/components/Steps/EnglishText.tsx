import type { Lesson } from '../../types'; import { SpeakingRecorder } from './SpeakingRecorder';
export function EnglishText({lesson}:{lesson:Lesson}){const paragraphs=lesson.englishText.trim().split('\n').filter(p=>p.trim());return <div className="content-card"><h2 className="card-title">英文复述挑战</h2><div className="speaking-brief"><b>训练任务</b><p>阅读英文原文，提取三个核心观点。收起原文后，用自己的语言完成回答。</p></div><details className="source-text" open><summary>查看 / 收起英文原文</summary><div className="english-text-scrollable">{paragraphs.map((p,i)=><p key={i}>{p.trim()}</p>)}</div></details><SpeakingRecorder lesson={lesson}/></div>}
